package ship;

public class bulkcarriership extends ship {
    public BulkCarrierShip(String name, double cargoCapacity, int cranes, int layers, int hatches) {
        super(name, cargoCapacity, cranes, layers, hatches);
    }
}
