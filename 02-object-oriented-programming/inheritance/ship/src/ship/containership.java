package ship;

public class containership extends ship {
    public ContainerShip(String name, double cargoCapacity, int cranes, int layers, int hatches) {
        super(name, cargoCapacity, cranes, layers, hatches);
    }
}
